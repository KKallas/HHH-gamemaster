# ---- Python ----
__pycache__/
*.py[cod]
.venv/
venv/
env/
.platformio/
.python-version
.pytest_cache/

# ---- hub / machine runtime state (regenerated locally) ----
hub-settings.json
# passwords + cookie secret — generated on first run, never committed
hub-config.json
# import staging/backup folders
sandboxes/*/prototypes.prev/
sandboxes/*/.import-new/
# generated game score logs
high-score-log.jsonl
sandboxes/*/prototypes/*/high-score-log.jsonl

# ---- OS / editor ----
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Note: each machine also has its own .gitignore for its runtime *.json
# (camera-settings.json, areas.json, settings.json, ...).
